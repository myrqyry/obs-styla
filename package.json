{
  "name": "obs-styla-monorepo",
  "private": true,
  "packageManager": "pnpm@10.20.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "install-deps": "pnpm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "turbo": "^2.6.1"
  }
}
